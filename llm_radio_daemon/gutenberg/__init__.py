"""Project Gutenberg テキストの取得と前処理（英語版の朗読コーナーの原典）。

日本語版の ``aozora/`` に対応する層。朗読パートは LLM を一切通さないので、
ここでやることも青空文庫版と同じ「原文を取ってきて、TTS 投入用の ``speech_text`` と
画面字幕用の ``display_text`` に分けて :class:`Chunk` を並べる」だけ。

なぜ ``aozora/`` を書き換えずに隣へ足したか
------------------------------------------
取得プロトコル・本文の書式・権利の判断が、青空文庫と Gutenberg で何ひとつ共通して
いない（ZIP＋Shift_JIS＋独自注記記法 / gz カタログ＋UTF-8＋``***`` マーカー）。
共通の抽象を1つ被せると、両方の分岐を抱えた層が1つ増えるだけで薄くならない。
プロンプト本文と同じ方針（language.py §5.1）で**日本語版はそのまま残し、
英語版を隣に置いて ``literary_reading/corner.py`` が言語で選ぶ**形にした。

``Chunk`` だけは共有している
---------------------------
``literary_reading`` の ``session.py`` / ``comment.py`` / ``corner.py`` は言語に依らず
同じ型を扱うので、``Chunk`` を2つ作ると上の層が両方を知る必要が出てしまう。
定義は ``aozora`` 側に置いたまま（動かすと日本語版の import が動く）ここから再公開する。

権利について
-----------
gutenberg.org が配っているのは**米国でパブリックドメインのもの**で、青空文庫の
「作品著作権フラグ」に当たる列は ``pg_catalog.csv`` に無い（:mod:`fetch` の
``is_public_domain_text`` が本文ヘッダ側で二重チェックする）。日本から聴く場合の
保護期間は米国と一致しないので、**この番組は個人が自宅で聴くものという前提**で
そのまま使っている。配信する用途に広げるなら、ここを作品ごとに見直すこと。
"""

from __future__ import annotations

from ..aozora import Chunk, Work

__all__ = ["Chunk", "Work"]
